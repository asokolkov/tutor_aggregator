image: mcr.microsoft.com/dotnet/sdk:6.0

stages:
  - build
  - test

build:
  stage: build
  script:
    - dotnet build -c Release
  only:
    - merge_requests
    - master

test job:
  stage: test
  script:
    - dotnet test -c Release
  only:
    - merge_requests
    - master